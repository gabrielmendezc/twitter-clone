import styled from 'styled-components'

export const ConfirmAccountModalWrapper = styled.div`
  padding: 2em;
  display: flex;
  justify-content: space-between;
  align-items: center;
  background-color: var(--complementary-3);
  strong {
    font-size: 1.5rem;
    margin-right: 8px;
  }

  svg {
    min-width: 15px;
    cursor: pointer;
    fill: black;
  }
`
